(function( ColorZebra, $, undefined ) {
    ColorZebra.LinearSpline = function(points) {
        // Check that lightness is increasing
        
        this.getSplineCoords = function(t) {
            
        }
        
        this.getColorForLightness = function(t) {
            
        }
    }
}( window.ColorZebra = window.ColorZebra || {}, jQuery ));